# 更新日志

## v26.8.15.1（2026-08-15）

### 新增
- 自动回收覆盖装备栏：新增 `RecycleRules > RecycleEquipped`（默认开启），自动回收时同样处理主手/副手/手中（装备栏）中的物品，与背包页合并计数与保底保留；关闭则只处理背包/背心/衬衫/裤子页

## v26.8.12.1（2026-08-12）

### 新增
- **接入插件管理器的配置分类导航**：全部配置项添加 `Unturned.Category:分类名` 标签（通用设置 / 自动合成 / 自动回收 / 自动修复），在插件管理器中按分类分组显示，点击左侧分类按钮切换查看对应配置，互不干扰

## v26.8.11.2（2026-08-11）

### 新增
- Alt+左键快捷标记：背包界面 Alt+左键点击物品 = 标记/取消标记自动回收；合成界面 Alt+左键点击配方 = 标记/取消标记自动合成（立即写入配置并生效）
- 处理时机调整：合成/回收延迟到物品进入背包 0.3 秒后执行，避免物品未落定就被处理
- 修复检测频率：手持主/副手武器时每 1 秒检测，未手持时每 5 秒
- 修复提示规则：主副手武器未手持时耐久不足也不提示（仍自动修复），仅手持且材料不足时才提示

## v26.8.11.1（2026-08-11）

### 变更
- 版本号格式调整：去掉「时」字段，改为 `年.月.日.第几版`（如 `26.8.11.1`）

## v26.8.11.12.1（2026-08-11）

首个公开版本。

### 新增
- 自动合成（主）：配方列表 `所属物品ID:配方编号`，拾取/获得物品时自动检查并合成；材料不足提示、充足自动合成（可多次）
- 保留一份原材料开关：每种材料至少保留 1 份，保留后仍够一批才合成
- 自动回收（辅）：指定物品自动拆解成废料（沿用游戏 Salvage 蓝图），无法拆解/数量不足可提醒
- 回收白名单/黑名单模式切换
- 回收保底保留 1 个选项
- 自动修复：耐久低于阈值自动修复主手/副手/背包物品，材料不足提示、可切换空手防损坏
- 拾取物品 ID 播报（Off/Popup/Chat 三档）
- 提示位置统一支持 Chat（聊天栏）/ Popup（屏幕中下方提示栏）/ Both
- 提示冷却限频（NotifyCooldownSeconds），防刷屏
- 定时全量扫描（ScanInterval），兜住任务奖励等无拾取事件的来源
- 配置热重载：游戏运行中修改 .cfg 约 5 秒自动生效
- PluginManager 兼容：物品/配方选择器标签、循环切换按钮标签
- 全套异常防护：任何错误写入 BepInEx 日志，插件不静默失效
